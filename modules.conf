# id|title|installer|environment flag|default|web path
# Keep the core line first. Add every future optional module on one new line.
ipam|IP Address Management|install.sh||ON|/
network-map|Network Map|modules/network-map/install.sh|EMS_NETWORK_MAP_ENABLED|ON|/network-map/
