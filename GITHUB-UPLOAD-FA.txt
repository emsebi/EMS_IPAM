تنظیمات ساخت Repository

Owner: emsebi
Repository name: EMS_IPAM
Visibility: Public
Add README: Off
Add .gitignore: No .gitignore
Add license: No license فعلاً

پس از ساخت Repository، محتویات پوشه EMS_IPAM را مستقیماً در ریشه Repository
قرار دهید؛ خود پوشه EMS_IPAM را به‌عنوان یک زیرپوشه اضافه نکنید.

پوشه‌های docker-app، windows-client و docs را همراه با همه فایل‌های داخلشان
آپلود کنید. تصویر README در مسیر docs/ems-ipam-preview-v0.4.0.png قرار دارد.
نام تصویر نسخه‌دار است تا GitHub تصویر قدیمی را از Cache نمایش ندهد.

ساختار اصلی Repository:

docker-app/
docs/
windows-client/
.env.example
.gitignore
compose.yml
install.sh
portainer-stack.yml
README.md
VERSION

شاخه پیش‌فرض باید main باشد.

دستور نصب پس از کامل‌شدن Upload:

curl -fsSL https://raw.githubusercontent.com/emsebi/EMS_IPAM/main/install.sh | sudo bash

توجه: تا زمانی که تمام فایل‌ها، به‌خصوص install.sh، compose.yml و docker-app،
در Repository قرار نگرفته‌اند این دستور را اجرا نکنید.
