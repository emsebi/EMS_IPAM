EMS IPAM Base v1.5.1-base-fix

ZIP را Extract کنید و محتویاتش را مستقیماً در Root مخزن زیر قرار دهید:
https://github.com/emsebi/EMS_IPAM

سپس Commit/Push و روی Ubuntu فقط این دستور را اجرا کنید:
curl -fsSL https://raw.githubusercontent.com/emsebi/EMS_IPAM/main/install.sh | sudo bash

منو:
1) Install
2) Update
3) Uninstall application (keep database)
4) Uninstall application + database

Update ایمن:
- PostgreSQL named volume حذف نمی‌شود.
- /var/lib/ems-ipam/.env حفظ می‌شود.
- قبل از Update یک DB backup معتبر و غیرخالی اجباری است.
- اگر Backup ساخته نشود Update قبل از تغییر برنامه متوقف می‌شود.
- اگر Health Check نسخه جدید Fail شود، فایل‌های Application rollback می‌شوند.

چک‌لیست تست: docs/TEST-CHECKLIST-FA.md
